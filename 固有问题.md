# 简历复现固有问题

## 问题

AI 生成的 LayoutTree 与手动复原的布局树在三个维度存在系统性差异，导致复现质量不理想。

### 1. 模块粒度粗糙

AI 倾向输出「标题 + 单一 content blob」的扁平结构：

```
module → heading (ref: modules.0.title) + text (ref: modules.0.content)
```

手动复原将每条记录拆为多层独立节点（日期、公司、角色、描述各自独立，可分别控制样式和位置）。AI 的 content blob 到达 translateLayoutTree 时已丢失排版细节。

### 2. 排版手段单一

AI 依赖 `gap` 做间距、`grid` 做多栏、示例的左右布局做默认排列。手动复原使用 `justifyContent: "space-between"` + 固定 `height` + 零 `gap` + 多层 flex 嵌套来精确控制位置。这些排版微调不在当前 prompt 的词汇表覆盖范围内。

### 3. 内容归属偏差

AI 对模块划分（哪些内容属于教育背景、哪些属于工作经历）、字段命名（date/company/role 等）的判断与人工不一致。

## 已采取措施

- prompt 新增多条目示例（entries 结构），展示 date/company/role/description 拆分 + justifyContent 排版
- 新增规则 3-5 显式要求逐层表达排版、entries 拆分、禁止简化为单 content 字符串
- `resolveRef` 扩展支持 `modules.N.entries.M.field` 深层引用路径

## 待验证

- 视觉模型（qwen-vl-max 等）是否按新示例输出多条目结构
- `translateLayoutTree` 是否完整支持嵌套 entries 的 LayoutTree
- 实际简历图片的复现效果是否改善

## 可能的后续方向

- 若视觉模型仍输出扁平结构：考虑两步流程（先 OCR 提取文本 → 第二模型根据 OCR 结果构建 LayoutTree）
- 若排版精度仍不足：在 prompt 中增加「原图标注示例」（如箭头标注左右分布、固定列宽等视觉提示）
- 若模块划分持续不准：引入用户干预点（导入后允许用户手动调整模块归属，再触发 AI 重排）
