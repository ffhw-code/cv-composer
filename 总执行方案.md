# cv-composer 总执行方案

## 项目目标

### 层级一：手动编辑
用户可拖拽控件、调整样式、输入内容，基于 HTML/CSS 文档流自动排版，独立完成简历制作。所有功能不依赖 AI 可用。

### 层级二：AI 代理
用户接入自选大模型 API 后，AI 作为 agent 通过 function calling 可靠调用编辑器的全部操作：创建模块、修改内容与样式、调整布局、应用模板、导出 PDF。
- **衡量标准**：前端删除 JSON 修复、幻觉防御等补丁代码后，AI 操作成功率仅需正常重试即可恢复。

### 层级三：AI 复现
上传简历图片后，AI 提取内容与排版信息，忠实重建为可编辑格式。
- **衡量标准**：重建结果在模块划分、内容归属、布局结构、视觉层次上与原图高度一致，不退化到模板近似替换。

---

## 阶段一：AI 可靠性基础设施（P0）

> 核心原则：每个改动同步写测试。改哪个文件，同时产哪个测试。

### 1. 拆分 tool 粒度

**问题**：当前 `execute_commands` 是唯一操作类 tool，要求 LLM 在一次调用中输出深度嵌套、带 tempId 引用、需满足容器铁律的指令数组——这是所有可靠性问题的根源。

**方案**：在 `aiPrompt.ts` 中定义以下 tool，每个 tool 参数扁平、类型明确：

| Tool | 关键参数 | 语义 |
|---|---|---|
| `add_text` | `content, style?` | 添加文本框 |
| `add_heading` | `content, style?` | 添加标题 |
| `add_list` | `content, style?` | 添加列表 |
| `add_image` | `content?, style?` | 添加图片 |
| `add_flex` | `children: string[], direction?, gap?, style?` | 弹性容器，children 为已有模块 id |
| `add_grid` | `children: string[], columns?, gap?, style?` | 网格容器 |
| `add_header` | `styleId` | 简历头，children 由前端从 styleRegistry 构建 |
| `add_module` | `styleId, title, content` | 模块，heading+text 子控件由前端自动生成 |
| `set_content` | `id, content` | 修改内容 |
| `set_style` | `id, style` | 修改样式 |
| `set_property` | `id, property, value` | 修改单个 CSS 属性 |
| `remove_module` | `id` | 删除 |
| `move_module` | `id, parent_id?, index` | 移动 |
| `duplicate_module` | `id` | 复制 |
| `apply_template` | `name` | 应用模板 |
| `export_pdf` | `{}` | 导出 PDF |
| `execute_skill` | `name, params?` | 高级技能（保留） |

**新建** `src/engine/toolHandlers.ts`：每个 tool 对应一个纯函数，接收参数 + 当前 modules，返回 `{newModules, errors}` 或技能结果。内部调用 `commandExecutor` 的对应 action，不对 LLM 暴露 Command 结构。

**修改** `src/components/AI/ChatPanel.tsx`：tool 调用路由改为查找 `toolHandlers` 中的对应函数，删除 `tryExecuteCommandsFromText`、`fixJson`、`extractJsonArrays` 等全部防御逻辑。

**测试**：
- 新建 `src/engine/toolHandlers.test.ts`：每个 handler 的输入输出验证
- 扩展现有 `src/engine/commandExecutor.test.ts`：覆盖新结构化错误格式

**注意**：`add_flex`/`add_grid` 的 `children` 参数要求传入已有模块 id，而新模块的 id 在上一轮 tool 调用返回后才可知。这意味着 LLM 无法在同一次回复中先创建子模块再将其包装到 flex/grid 容器中。为此新增两个工具消除此 gap：

| `add_flex_inline` | `children: InlineChildDef[], direction?, gap?, style?` | 弹性容器，children 内联定义为 `{type, content/ref, style?}` |
| `add_grid_inline` | `children: InlineChildDef[], columns?, gap?, style?` | 网格容器，同上 |

`InlineChildDef` 类型仅需 `type` 字段，`content` 或 `ref` 二选一。handler 内部直接生成子模块，一次 tool 调用完成容器+子控件创建。

### 2. 画布状态自动注入 system prompt

**方案**：在 `ChatPanel.handleSend` 中，每次组装消息前调用 `getCanvasState()`，将当前画布所有模块的 id、type、styleId、content 摘要拼入 system prompt 的 `## 当前画布` 区块。LLM 天然知晓所有模块标识，不需要再主动调用 `get_canvas_state` tool。删除该 tool 定义。

```
## 当前画布
- header-1 (header, header-classic)
  - img-2 (image): content=""
  - name-3 (text): content="张三"
- mod-4 (module, module-card)
  - h-5 (heading): content="教育背景"
  - t-6 (text): content="清华大学 · 计算机科学"
```

**预算控制**：采用「全量优先 + 按需截断」策略，分为两档：

**正常档（≤3000 token）**：全量注入所有模块的 id、type、styleId、content 摘要，不做任何截断。典型简历（≤10 个模块）均可完整注入。
**超限档（>3000 token）**：按深度优先保留树结构（浅层 header、section 标题优先完整保留），深层叶子文本节点的 content 超过 100 字符时缩略为「前 80 字符 + …」。超限时在末尾附加 `(画布模块过多，部分内容已缩略)` 提示。

### 3. 声明式约束替代关键词 ruleBase

**问题**：`ruleBase.ts` 当前的关键词匹配逻辑不可靠——规则注入取决于用户措辞而非任务需求。

**方案**：收缩为固定约束清单，始终注入 system prompt，删除 `retrieveRules` 函数：

```
## 必须遵守的规则
1. 修改已有模块时，id 必须来自「当前画布」列表，禁止编造
2. 创建 resume header 使用 add_header，创建内容模块使用 add_module，禁止手动构造它们的 children
3. 样式属性仅使用: fontSize, fontWeight, color, backgroundColor, padding, margin, borderRadius, border, boxShadow, display, flexDirection, alignItems, justifyContent, gap, gridTemplateColumns, width, height, lineHeight, textAlign, objectFit
4. 颜色值统一用 #rrggbb，尺寸值统一用 px 单位
5. 单次回复可调用多个 tool，但须等待 tool 结果后再决定下一步
6. `add_flex`/`add_grid` 仅用于包装已存在的子模块；创建新的容器+子控件组合时使用 `add_flex_inline`/`add_grid_inline`
7. 单次用户消息最多重试 3 次 tool 调用；连续同一 tool 失败 2 次后不得再试，改为向用户报告具体错误并附原始 AI 输出供诊断
```

**测试**：新建 `src/engine/ruleBase.test.ts`，验证约束列表结构完整性。

### 4. 自纠正错误反馈

**方案**：`toolHandlers.ts` 中每个 handler 的错误返回改为结构化格式，包含三要素：

```
{
  code: "MISSING_CHILDREN",
  message: "add_module 创建了模块但未提供内容",
  fix: "请同时提供 title 和 content 参数: add_module(styleId: 'module-card', title: '教育背景', content: '...')"
}
```

ChatPanel 重试时将错误对象原样传给 LLM，LLM 根据 `fix` 字段自纠正。

**重试上限**：在 `ChatPanel.tsx` 中实现硬限制——每个用户消息最多触发 3 次自纠正重试，连续同一 tool 相同 error code 失败 2 次后不再重试。超过限制时在 UI 展示原始 AI 输出供用户诊断，而非静默消耗 token。

---

## 阶段二：简历忠实复现（P1）

### 1. 删除模板回退路径

**问题**：`import-resume` 技能中 `parsed.layoutTree` 不存在时回退到 `buildResumeCommands`，这条路径用固定模板重建，破坏了忠实复现的目标。

**方案**：删除 `skillExecutor.ts` 中 `buildResumeCommands` 函数（约 130 行）及其所有辅助函数和调用分支。`import-resume` 仅接受 LayoutTree 路径，解析失败时直接向用户报错并附原始 AI 输出供诊断，不降级为模板重建。

### 2. 重新设计解析 prompt

**问题**：当前 prompt 要求 AI 输出扁平的 `ParsedResume`，LayoutTree 由另一条路径尝试提取。两轮调用导致信息割裂。

**方案**：`resumeParser.ts` 中新的 `buildPrompt` 要求视觉模型在单次调用中输出完整结构——LayoutTree 描述排版，data 存放文本数据，通过 ref 关联：

```
{
  "layoutTree": {
    "header": {
      "type": "flex", "direction": "row", "gap": "20px", "padding": "24px",
      "style": {"backgroundColor": "#f8fafc"},
      "children": [
        {"type": "image", "ref": "photo", "style": {"width": "100px"}},
        {"type": "flex", "direction": "column", "children": [
          {"type": "text", "ref": "name", "style": {"fontSize": "24px"}},
          {"type": "grid", "columns": 2, "children": [
            {"type": "text", "ref": "jobTitle"},
            {"type": "text", "ref": "email"}
          ]}
        ]}
      ]
    },
    "modules": [...]
  },
  "data": {
    "name": "张三", "jobTitle": "产品经理", "email": "z@e.com",
    "photo": "",
    "modules": [
      {"title": "教育背景", "content": "清华大学..."},
      {"title": "工作经历", "content": "某公司..."}
    ]
  }
}
```

prompt 中明确列举可用节点类型、CSS 属性词汇表、容器 children 规则。

**修改** `src/utils/resumeParser.ts` 的 `buildPrompt`、`ParsedResume`、`ParsedModule` 类型定义。删除旧的 `ResumeLayout`、`ModuleLayout` 接口，其功能由 LayoutTree 替代。

**JSON 解析失败恢复**：视觉模型输出未包裹在 markdown code fence 中或 JSON 不合法时，`extractJson` 在尝试基础清洗后仍失败，则发起一次重试——将原始输出和解析错误信息拼接进一条新消息发给同一模型，要求其修正 JSON 格式。重试仅一次，若仍失败则抛出错误并在 UI 展示原始 AI 输出。修改 `src/utils/resumeParser.ts` 的 `callApi`：在 JSON 解析失败分支中加入单次修正重试逻辑。

**测试**：新建 `src/utils/resumeParser.test.ts`，用 mock AI 返回验证新 prompt 输出的解析路径。

### 3. LayoutTree 规范化层

**新建** `src/engine/layoutTreeNormalizer.ts`，`normalizeLayoutTree(tree, data)` 做：

- 裁剪不在允许类型集合中的节点
- 容器节点（flex/grid）children 为空时插入一个 text 占位
- 补全缺失样式默认值（fontSize: 15px, color: #334155, gap: 8px 等）
- 解析所有 ref 指向的 data 字段，将值写入节点 content 属性
- 合并只有单个子节点且无额外样式的 flex 嵌套
- 确保所有 type 字段非空，缺失时根据上下文推断，无法推断时回退为 `text`

**保守原则**（防止过度修正丢失原始布局意图）：
- 合并 flex 嵌套时仅合并「无 style 属性」或「style 对象为空」的中间层 flex 节点；任何带有 `padding`、`margin`、`backgroundColor`、`border`、`boxShadow`、`alignItems`、`justifyContent` 的 flex 均视为有意为之，保留不合并
- 补全默认值仅补充缺失字段，不覆盖 AI 已经输出的值——即使该值在 A4 上下文中不合理，也予以保留（不合理值交给溢出压缩阶段处理）
- 插入 text 占位时设置 `content: ""` 并标记 `_placeholder: true`，`translateLayoutTree` 渲染时识别该标记，若无 data 对应 ref 则渲染空段落而非填充默认文本

`translateLayoutTree` 现在接收规范化后的 LayoutTree，不需要再做防御判断。

**测试**：新建 `src/engine/layoutTreeNormalizer.test.ts`，覆盖空 children、缺字段、无效类型、单子 flex 冗余、ref 解析失败、带样式 flex 不合并、默认值不覆盖已有值等场景。

### 4. 溢出压缩前置到 LayoutTree 层

**方案**：`applyOverflowCompression` 从接受 `Command[]` 改为接受 `LayoutTreeNode[]`。在 `translateLayoutTree` 之前调用，直接在 LayoutTree 上估算高度并缩放间距和页边距。

`import-resume` 技能的处理流程变为：parse → normalize → compress → translate → execute。

---

## 阶段三：AI 辅助能力增强（P2）

### 1. smart-fill 重构

**问题**：当前标签匹配用 content 前 20 字截断，精度低导致匹配失败频繁。

**方案**：`smart-fill` 改为遍历画布树，生成模块路径列表（如 `header > name-text`、`edu-module > edu-heading`），每个路径附带当前内容和类型。LLM 返回 `{path, content}` 数组，前端精确定位模块 id。替换现有标签匹配方式。

**测试**：随 `skillExecutor.test.ts` 新建一道覆盖。

### 2. 评估-建议-执行闭环

**方案**：`evaluate-resume` 输出改为结构化建议列表，每条建议携带可执行的操作描述。用户可在界面中点击建议直接应用（前端将建议翻译为 tool 调用），也可选择部分建议执行。

---

## 阶段四：质量加固（P3）

### 1. 补充稳定模块的测试

- 新建 `src/engine/templates.test.ts`：`loadTemplate` 返回值结构完整性，`simple`/`classic` 模板中所有容器节点都有非空 children
- 新建 `src/components/AI/ChatPanel.test.tsx`：tool 路由集成测试（mock LLM 调用）
- 新建 `src/utils/export.test.ts`：`exportPDF` 的 DOM 操作测试

### 2. 多 provider 适配验证

对 OpenAI（gpt-4o）、阿里百炼（qwen-plus）、自定义兼容接口分别验证：tool 调用往返正确性、视觉模型图片解析行为、错误码翻译准确性。

**修改** `src/utils/aiConfig.ts`：扩展 provider 差异配置。

### 3. UX 完善

- ChatPanel 增加 tool 执行过渡态（`ToolExecuting` 组件，显示当前工具名称和状态）
- 解析失败时展示原始 AI 输出供诊断，而非仅一行错误文本
- 模块拖入画布加插入位置指示动画

---

## 文件变更总览

| 文件 | 阶段 | 操作 |
|---|---|---|
| `src/engine/toolHandlers.ts` | P0 | **新建** — 全部 tool 处理函数 |
| `src/engine/toolHandlers.test.ts` | P0 | **新建** |
| `src/engine/aiPrompt.ts` | P0 | **重写** — 新 tool 定义 + 新 system prompt |
| `src/engine/ruleBase.ts` | P0 | **重写** — 固定约束列表，删除关键词匹配和 `retrieveRules` |
| `src/engine/ruleBase.test.ts` | P0 | **新建** |
| `src/components/AI/ChatPanel.tsx` | P0 | **重写** — tool 路由、状态注入、删除防御代码 |
| `src/engine/commandExecutor.ts` | P0 | **修改** — 错误格式改为结构化（code + message + fix） |
| `src/engine/commandExecutor.test.ts` | P0 | **扩展** — 覆盖新错误格式 |
| `src/engine/layoutTreeNormalizer.ts` | P1 | **新建** |
| `src/engine/layoutTreeNormalizer.test.ts` | P1 | **新建** |
| `src/utils/resumeParser.ts` | P1 | **修改** — 新 prompt、删除 `ResumeLayout`/`ModuleLayout` 类型、合并 `ParsedResume` 结构 |
| `src/utils/resumeParser.test.ts` | P1 | **新建** |
| `src/engine/skillExecutor.ts` | P1/P2 | **修改** — 删除 `buildResumeCommands`、重写 `import-resume` 流程、重写 `smart-fill`、更新 `evaluate-resume` |
| `src/engine/skillExecutor.test.ts` | P1/P2 | **新建** |
| `src/engine/templates.test.ts` | P3 | **新建** — 稳定模块补测 |
| `src/components/AI/ChatPanel.test.tsx` | P3 | **新建** |
| `src/utils/export.test.ts` | P3 | **新建** — 稳定模块补测 |
| `src/utils/aiConfig.ts` | P3 | **修改** — 扩展 provider 差异配置 |
| `src/engine/templates.ts` | — | 不改 |
| `src/engine/layoutEngine.ts` | — | 不改 |
| `src/store/useResumeStore.ts` | — | 不改 |
| `src/store/styleRegistry.ts` | — | 不改 |
