# cv-composer 项目指标记录

用途有两个：一是为「某次改动是否带来优化、优化多少」提供可对比依据；二是沉淀可复现的量化素材，供项目描述使用。
核心原则：先确认噪声，再下结论。低于噪声阈值的差异一律记为「无显著变化」。

## 一、测量协议

- **环境固定**：同一台机器、同一 Node 版本、同一条命令；测量期间不做其它重负载操作。
- **重复采样**：每项指标连续测 N 次（N ≥ 3），丢弃第 1 次（冷启动），取中位数。
- **计时口径**：脚本用 `date +%s%N` 记录 npm 命令的墙钟时间；测试内部用时取 vitest 自报的 `tests` 字段。
- **绑定 commit**：每条记录必须写明 commit（base→head）、命令与机器，不能只记日期，否则事后无法解释。
- **环境隔离**：CI（GitHub Actions runner）的耗时噪声远大于本机，两者不混用、不互相比较。

## 二、测量环境

| 项 | 值 |
|---|---|
| CPU | AMD Ryzen 7 8845H（16 线程） |
| 内存 | 13 GiB |
| OS | Linux 6.6.87.2-microsoft-standard-WSL2 |
| Node / npm | v24.15.0 / 11.12.1 |
| 基线 commit | 7dcb3bf |
| 采集日期 | 2026-09-10 |

注意：`npm run build` 中的 `tsc -b` 是增量编译，缓存位于 `node_modules/.tmp/tsconfig.app.tsbuildinfo`。改过源码后首次构建会偏慢，因此统一丢弃第 1 次采样。

## 三、基线原始采样（commit 7dcb3bf）

### 构建：`npm run build`（tsc -b + vite build）

| 采样 | 墙钟 (ms) | JS 字节 | JS gzip | CSS 字节 | CSS gzip | JS sha256 前 12 位 |
|---|---|---|---|---|---|---|
| 1 | 4317 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 2 | 3583 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 3 | 4057 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 4 | 4119 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 5 | 3855 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |

产物体积在 5 次构建中逐字节一致（sha 相同），说明**体积指标噪声为 0，可直接作为硬门禁**；构建耗时有明显波动。

### 测试：`npm test`

| 采样 | 墙钟 (ms) | vitest Duration | tests 用时 | 用例数 |
|---|---|---|---|---|
| 1 | 1872 | 1.31s | 400ms | 240 passed (14 files) |
| 2 | 1515 | 993ms | 409ms | 240 passed (14 files) |
| 3 | 1484 | 980ms | 399ms | 240 passed (14 files) |
| 4 | 1508 | 983ms | 398ms | 240 passed (14 files) |
| 5 | 1569 | 1.02s | 428ms | 240 passed (14 files) |

用例数在 5 次采样中完全一致；`tests` 用时是本次基线里最稳的计时指标。

### 静态检查

| 命令 | 采样 1 (ms) | 采样 2 (ms) | 采样 3 (ms) | 结果 |
|---|---|---|---|---|
| `npm run lint` | 5609 | 4543 | 4936 | exit 0，0 问题 |
| `npx tsc -b --force` | 3776 | 3235 | 3231 | exit 0，0 错误 |

两项的首次采样都明显偏慢，与冷启动/缓存状态一致。

## 四、噪声区间与判定阈值（本次基线的核心产出）

| 指标 | 中位数 | 实测波动范围 | 相对中位的跨度 | 建议判定阈值 |
|---|---|---|---|---|
| JS 产物体积 | 836185 B | 逐字节一致 | 0% | 任何字节变化都真实 |
| JS gzip 体积 | 258809 B | 逐字节一致 | 0% | 任何字节变化都真实 |
| 用例数 | 240 | 一致 | 0% | 任何变化都真实 |
| tests 内部用时 | 400 ms | 398 – 428 ms | 7.0% | > 10%（主指标） |
| 构建墙钟 | 4057 ms | 3583 – 4317 ms | 18.1% | > 20% |
| tsc --force 墙钟 | 3235 ms | 3231 – 3776 ms | 16.8% | > 20% |
| 测试墙钟 | 1515 ms | 1484 – 1872 ms | 25.6% | > 25%（仅记录，不作判定） |
| lint 墙钟 | 4936 ms | 4543 – 5609 ms | 21.6% | > 40%（样本仅 3 个） |

阈值取「实测跨度 × 1.1 ~ 1.9」，即必须明显超过当前观测到的噪声才算信号。由此得到四条使用规则：

1. 体积类与用例数指标是精确值，可以直接写「从 X 降到 Y，减少 Z%」。
2. 耗时类指标必须超过上表阈值才可断言优化。例如构建从 4057 ms 变成 3900 ms 属于噪声，不能称为优化。
3. 构建墙钟最初按 > 10% 设定偏乐观（实测跨度已达 18.1%），已上调至 > 20%；测试墙钟实测跨度 25.6%，上调至 > 25% 并降级为辅助指标（跨度主要由首采冷启动贡献，丢弃首采后仅 5.6%，但 5 个样本不足以据此收紧）。
4. 主指标仍是 `tests` 内部用时（跨度 7.0%）；墙钟时间仅作辅助参考。

## 五、指标记录表（后续每次改动追加一行）

| 日期 | commit 区间 | JS 字节 | JS gzip | 构建中位 (ms) | 测试中位 (ms) | tests 中位 (ms) | 用例数 | lint | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-10 | 7dcb3bf（基线） | 836185 | 258809 | 4057 | 1515 | 400 | 240 | 0 | 噪声基线，协议见第一节 |

## 六、纯函数热路径基准（vitest bench）

运行方式：`npx vitest bench`（11 个 bench，约 10 秒，未引入任何新依赖）。覆盖 `layoutEngine` / `layoutTreeNormalizer` / `moduleUtils` 三个纯函数模块。

设计说明：`normalizeLayoutTree` 与 `applyOverflowCompression` 都会**就地修改输入**，因此每次迭代在 callback 内重新构造输入，确保测到的是「首次规范化」路径；为此单列一条「对照：仅构造输入」，净耗时取两者之差。其余被测函数是纯函数，输入在模块加载时构造一次。

### 三轮结果（hz = ops/sec，同机连续测量）

| bench | run1 | run2 | run3 | 中位 (ops/s) | 中位单次耗时 | run 间跨度 | 组内 rme | 建议判定阈值 |
|---|---|---|---|---|---|---|---|---|
| `computeLayout` 12 模块 | 7,338,741 | 7,193,879 | 7,528,357 | 7,338,741 | 0.1 µs | 4.6% | ±0.84~4.18% | > 5% |
| `computeLayout` 60 模块 | 2,310,932 | 2,239,136 | 2,249,939 | 2,249,939 | 0.4 µs | 3.2% | ±5.75~7.30% | > 5% |
| `findModuleById`（命中末节点） | 2,187,720 | 2,172,676 | 2,215,212 | 2,187,720 | 0.5 µs | 1.9% | ±0.24~0.33% | > 3% |
| `findParentById`（命中末节点） | 2,161,288 | 2,129,879 | 2,039,538 | 2,129,879 | 0.5 µs | 5.7% | ±0.21~0.28% | > 6% |
| `getAllModuleIds` | 986,679 | 726,437 | 679,059 | 726,437 | 1.4 µs | 42.3% | ±0.86~2.42% | 不宜单轮判定 |
| `exportLayoutTree` | 259,022 | 254,690 | 303,574 | 259,022 | 3.9 µs | 18.9% | ±0.79~0.98% | > 20% |
| `getCanvasStateSummary`（典型 40 节点） | 48,438 | 34,213 | 43,095 | 43,095 | 23.2 µs | 33.0% | ±1.26~1.60% | > 35% |
| `getCanvasStateSummary`（长简历截断） | 40,133 | 40,420 | 33,360 | 40,133 | 24.9 µs | 17.6% | ±0.96~1.32% | > 20% |
| `normalizeLayoutTree`（含构造） | 37,155 | 38,760 | 36,280 | 37,155 | 26.9 µs | 6.7% | ±1.61~1.62% | > 8% |
| 对照：仅构造输入 | 443,471 | 477,710 | 498,784 | 477,710 | 2.1 µs | 11.6% | ±0.51~0.54% | > 12% |
| `applyOverflowCompression`（含构造与规范化） | 11,757 | 11,641 | 10,935 | 11,641 | 85.9 µs | 7.1% | ±2.09~2.17% | > 8% |

### 结论

1. **组内 rme 严重低估真实波动**：tinybench 报告的 rme 最大只有 ±7.3%，而同一 bench 的 run 间跨度最高达 42.3%（`getAllModuleIds`）。判定改动必须用 run 间跨度，不能引用 rme。
2. **各 bench 的可用性差异极大**：`computeLayout`、`findModuleById`、`normalizeLayoutTree` 跨度都在 7% 以内，适合做判定依据；`getAllModuleIds`（42%）与 `getCanvasStateSummary` 典型规模（33%）噪声过大，只能看大方向。
3. **净耗时拆分**：`normalizeLayoutTree` 的 26.9 µs 中含 2.1 µs 输入构造（约 8%），净规范化约 24.8 µs——构造开销占比不高，不影响该指标对改动的敏感度。
4. **负结果同样有价值**：`computeLayout` 只要 0.1~0.4 µs，分页计算不是性能瓶颈，不值得投入优化。
5. **最值得关注的热路径**：`getCanvasStateSummary` 典型规模 23.2 µs，是 AI 每轮请求都要跑一遍的函数，且比 `findModuleById` 慢约 1.7 万倍；优化它的收益远大于优化树查找。

### 使用建议

- 正式判定改动时跑 ≥ 5 轮取中位数，阈值取上表；低于阈值记为「无显著变化」。
- 测量期间避免并行跑其它重负载任务；本次三轮为连续背靠背执行，已属于较理想条件，跨度仍有两位数百分比，说明该机器（WSL2）本身偏噪声。

## 七、后续待办

- [x] `vitest bench`：覆盖 `layoutEngine` / `layoutTreeNormalizer` / `moduleUtils` 纯函数热路径（见第六节）
- [ ] `scripts/metrics.mjs`：自动采集构建/测试/lint 指标，写 `metrics/<sha>.json` 并由脚本生成第五节的表格
- [ ] AI 工具调用埋点：成功率、重试轮数、单轮耗时、token 消耗、每轮注入的 prompt 体积
