# cv-composer 项目指标记录

用途有两个：一是为「某次改动是否带来优化、优化多少」提供可对比依据；二是沉淀可复现的量化素材，供项目描述使用。
核心原则：先确认噪声，再下结论。低于噪声阈值的差异一律记为「无显著变化」。

## 一、测量协议

- **环境固定**：同一台机器、同一 Node 版本、同一条命令；测量期间不做其它重负载操作。
- **重复采样**：每项指标连续测 N 次（N ≥ 3），丢弃第 1 次（冷启动），取中位数。
- **计时口径**：脚本用 `date +%s%N` 记录 npm 命令的墙钟时间；测试内部用时取 vitest 自报的 `tests` 字段。
- **绑定 commit**：每条记录必须写明 commit（base→head）、命令与机器，不能只记日期，否则事后无法解释。
- **环境隔离**：CI（GitHub Actions runner）的耗时噪声远大于本机，两者不混用、不互相比较。

## 二、测量环境

| 项 | 值 |
|---|---|
| CPU | AMD Ryzen 7 8845H（16 线程） |
| 内存 | 13 GiB |
| OS | Linux 6.6.87.2-microsoft-standard-WSL2 |
| Node / npm | v24.15.0 / 11.12.1 |
| 基线 commit | 7dcb3bf |
| 采集日期 | 2026-09-10 |

注意：`npm run build` 中的 `tsc -b` 是增量编译，缓存位于 `node_modules/.tmp/tsconfig.app.tsbuildinfo`。改过源码后首次构建会偏慢，因此统一丢弃第 1 次采样。

## 三、基线原始采样（commit 7dcb3bf）

### 构建：`npm run build`（tsc -b + vite build）

| 采样 | 墙钟 (ms) | JS 字节 | JS gzip | CSS 字节 | CSS gzip | JS sha256 前 12 位 |
|---|---|---|---|---|---|---|
| 1 | 4317 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 2 | 3583 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 3 | 4057 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 4 | 4119 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |
| 5 | 3855 | 836185 | 258809 | 30309 | 5846 | 67bf82ed573f |

产物体积在 5 次构建中逐字节一致（sha 相同），说明**体积指标噪声为 0，可直接作为硬门禁**；构建耗时有明显波动。

### 测试：`npm test`

| 采样 | 墙钟 (ms) | vitest Duration | tests 用时 | 用例数 |
|---|---|---|---|---|
| 1 | 1872 | 1.31s | 400ms | 240 passed (14 files) |
| 2 | 1515 | 993ms | 409ms | 240 passed (14 files) |
| 3 | 1484 | 980ms | 399ms | 240 passed (14 files) |
| 4 | 1508 | 983ms | 398ms | 240 passed (14 files) |
| 5 | 1569 | 1.02s | 428ms | 240 passed (14 files) |

用例数在 5 次采样中完全一致；`tests` 用时是本次基线里最稳的计时指标。

### 静态检查

| 命令 | 采样 1 (ms) | 采样 2 (ms) | 采样 3 (ms) | 结果 |
|---|---|---|---|---|
| `npm run lint` | 5609 | 4543 | 4936 | exit 0，0 问题 |
| `npx tsc -b --force` | 3776 | 3235 | 3231 | exit 0，0 错误 |

两项的首次采样都明显偏慢，与冷启动/缓存状态一致。

## 四、噪声区间与判定阈值（本次基线的核心产出）

| 指标 | 中位数 | 实测波动范围 | 相对中位的跨度 | 建议判定阈值 |
|---|---|---|---|---|
| JS 产物体积 | 836185 B | 逐字节一致 | 0% | 任何字节变化都真实 |
| JS gzip 体积 | 258809 B | 逐字节一致 | 0% | 任何字节变化都真实 |
| 用例数 | 240 | 一致 | 0% | 任何变化都真实 |
| tests 内部用时 | 400 ms | 398 – 428 ms | 7.0% | > 10%（主指标） |
| 构建墙钟 | 4057 ms | 3583 – 4317 ms | 18.1% | > 20% |
| tsc --force 墙钟 | 3235 ms | 3231 – 3776 ms | 16.8% | > 20% |
| 测试墙钟 | 1515 ms | 1484 – 1872 ms | 25.6% | > 25%（仅记录，不作判定） |
| lint 墙钟 | 4936 ms | 4543 – 5609 ms | 21.6% | > 40%（样本仅 3 个） |

阈值取「实测跨度 × 1.1 ~ 1.9」，即必须明显超过当前观测到的噪声才算信号。由此得到四条使用规则：

1. 体积类与用例数指标是精确值，可以直接写「从 X 降到 Y，减少 Z%」。
2. 耗时类指标必须超过上表阈值才可断言优化。例如构建从 4057 ms 变成 3900 ms 属于噪声，不能称为优化。
3. 构建墙钟最初按 > 10% 设定偏乐观（实测跨度已达 18.1%），已上调至 > 20%；测试墙钟实测跨度 25.6%，上调至 > 25% 并降级为辅助指标（跨度主要由首采冷启动贡献，丢弃首采后仅 5.6%，但 5 个样本不足以据此收紧）。
4. 主指标仍是 `tests` 内部用时（跨度 7.0%）；墙钟时间仅作辅助参考。
5. 用例数变化时，`tests` 用时必须按用例数归一化后再比较（否则「加了测试」会被误读成「测试变慢」）。

## 五、指标记录表（后续每次改动追加一行）

| 日期 | commit 区间 | JS 字节 | JS gzip | 构建中位 (ms) | 测试中位 (ms) | tests 中位 (ms) | 用例数 | lint | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| 2026-09-10 | 7dcb3bf（基线） | 836185 | 258809 | 4057 | 1515 | 400 | 240 | 0 | 噪声基线，协议见第一节 |
| 2026-09-11 | 514e453（AI 埋点） | 843848 | 261807 | 4144 | 1622 | 486 | 269 | 0 | 体积 +0.92% / gzip +1.16% 属真实增量；构建、测试墙钟均在噪声内；tests 用时需按用例数归一化（1.67 → 1.81 ms/用例，+8.4%，仍在阈值内） |

## 六、纯函数热路径基准（vitest bench）

运行方式：`npx vitest bench`（11 个 bench，约 10 秒，未引入任何新依赖）。覆盖 `layoutEngine` / `layoutTreeNormalizer` / `moduleUtils` 三个纯函数模块。

设计说明：`normalizeLayoutTree` 与 `applyOverflowCompression` 都会**就地修改输入**，因此每次迭代在 callback 内重新构造输入，确保测到的是「首次规范化」路径；为此单列一条「对照：仅构造输入」，净耗时取两者之差。其余被测函数是纯函数，输入在模块加载时构造一次。

### 三轮结果（hz = ops/sec，同机连续测量）

| bench | run1 | run2 | run3 | 中位 (ops/s) | 中位单次耗时 | run 间跨度 | 组内 rme | 建议判定阈值 |
|---|---|---|---|---|---|---|---|---|
| `computeLayout` 12 模块 | 7,338,741 | 7,193,879 | 7,528,357 | 7,338,741 | 0.1 µs | 4.6% | ±0.84~4.18% | > 5% |
| `computeLayout` 60 模块 | 2,310,932 | 2,239,136 | 2,249,939 | 2,249,939 | 0.4 µs | 3.2% | ±5.75~7.30% | > 5% |
| `findModuleById`（命中末节点） | 2,187,720 | 2,172,676 | 2,215,212 | 2,187,720 | 0.5 µs | 1.9% | ±0.24~0.33% | > 3% |
| `findParentById`（命中末节点） | 2,161,288 | 2,129,879 | 2,039,538 | 2,129,879 | 0.5 µs | 5.7% | ±0.21~0.28% | > 6% |
| `getAllModuleIds` | 986,679 | 726,437 | 679,059 | 726,437 | 1.4 µs | 42.3% | ±0.86~2.42% | 不宜单轮判定 |
| `exportLayoutTree` | 259,022 | 254,690 | 303,574 | 259,022 | 3.9 µs | 18.9% | ±0.79~0.98% | > 20% |
| `getCanvasStateSummary`（典型 40 节点） | 48,438 | 34,213 | 43,095 | 43,095 | 23.2 µs | 33.0% | ±1.26~1.60% | > 35% |
| `getCanvasStateSummary`（长简历截断） | 40,133 | 40,420 | 33,360 | 40,133 | 24.9 µs | 17.6% | ±0.96~1.32% | > 20% |
| `normalizeLayoutTree`（含构造） | 37,155 | 38,760 | 36,280 | 37,155 | 26.9 µs | 6.7% | ±1.61~1.62% | > 8% |
| 对照：仅构造输入 | 443,471 | 477,710 | 498,784 | 477,710 | 2.1 µs | 11.6% | ±0.51~0.54% | > 12% |
| `applyOverflowCompression`（含构造与规范化） | 11,757 | 11,641 | 10,935 | 11,641 | 85.9 µs | 7.1% | ±2.09~2.17% | > 8% |

### 结论

1. **组内 rme 严重低估真实波动**：tinybench 报告的 rme 最大只有 ±7.3%，而同一 bench 的 run 间跨度最高达 42.3%（`getAllModuleIds`）。判定改动必须用 run 间跨度，不能引用 rme。
2. **各 bench 的可用性差异极大**：`computeLayout`、`findModuleById`、`normalizeLayoutTree` 跨度都在 7% 以内，适合做判定依据；`getAllModuleIds`（42%）与 `getCanvasStateSummary` 典型规模（33%）噪声过大，只能看大方向。
3. **净耗时拆分**：`normalizeLayoutTree` 的 26.9 µs 中含 2.1 µs 输入构造（约 8%），净规范化约 24.8 µs——构造开销占比不高，不影响该指标对改动的敏感度。
4. **负结果同样有价值**：`computeLayout` 只要 0.1~0.4 µs，分页计算不是性能瓶颈，不值得投入优化。
5. **最值得关注的热路径**：`getCanvasStateSummary` 典型规模 23.2 µs，是 AI 每轮请求都要跑一遍的函数，且比 `findModuleById` 慢约 1.7 万倍；优化它的收益远大于优化树查找。

### 使用建议

- 正式判定改动时跑 ≥ 5 轮取中位数，阈值取上表；低于阈值记为「无显著变化」。
- 测量期间避免并行跑其它重负载任务；本次三轮为连续背靠背执行，已属于较理想条件，跨度仍有两位数百分比，说明该机器（WSL2）本身偏噪声。

## 七、AI 调用指标（埋点）

实现在 `src/utils/aiMetrics.ts`，事件落在浏览器 `localStorage`（key `cv-composer:ai-metrics:v1`，环形缓冲保留最近 500 条）。
脱敏原则：只记录体量与结果，不记录 prompt、回复正文或 API Key。

### 能测到什么

| 维度 | 指标 |
|---|---|
| 请求 | 成功率、失败分类（timeout / network / http）、HTTP 状态码、按渠道分布（chat / smart-fill / evaluate / polish / import-parse） |
| 工具 | 调用成功率、参数解析状态（正常 / 修复后可用 / 不可用）、按工具名的失败次数 |
| 重试 | 平均与最大重试次数每轮、涉及重试的请求数 |
| 性能 | 单轮延迟 P50 / P95 / 最大（只统计成功请求，避免超时值污染分位数） |
| 成本 | token 用量（prompt / completion / 累计，取自响应 `usage`）、每轮注入字符数与估算 tokens、工具 schema 体积 |
| 对话 | 每轮结果分布（成功 / 部分成功 / 失败）、请求轮次、整轮耗时 |

### 怎么取数

1. 在页面里跑场景（dev 与线上均可；线上 `console` 已被构建移除，所以用界面按钮而不是控制台）。
2. 点 AI 助手面板的「AI 指标」按钮 → 下载 `ai-metrics-<日期>.json`（含原始事件与摘要），同时把文本摘要复制到剪贴板。
3. 摘要贴进本文件对应章节，JSON 单独归档。

### 对比口径（重要）

- **基线必须绑定 commit**：埋点能力落在 `514e453`，它即是后续修复 AI 链路的 `before` 基准。
- 记录写成区间：`before = 514e453（N 次场景化回归）` → `after = <修复提交>`；两侧必须同一组场景、同一模型、同一台机器。
- 单次样本没有意义：至少覆盖 3 种工具、包含 1 次失败场景，同一场景组重复 20~30 次。
- 建议给基线提交打 tag（如 `ai-baseline-v1`），避免日后找不到对比起点。
- 已知限制：手工样本量小、受服务商侧波动影响；token 数据依赖服务商返回 `usage`，缺失时该项为空。

## 八、AI 调用改前基线（harness 采集 @ `8d94975`）

**采集方式**：`AI_BASELINE_KEY=*** npm run ai-baseline`

- **驱动方式**：`renderHook` 直接跑真实 `useAiChat`；每轮重置为固定 id 的种子画布、清空埋点、新建会话（不复用历史消息），保证 25 轮的 system prompt 与画布完全一致。
- **规模**：5 类场景（`generate-resume` / `add_module` / `set_content` / `set_style` / `tool-error-retry`）× 5 次 = 25 轮；每场景跑完即落盘，中途中断也保留已采集数据。
- **环境**：与第二节同一台机器；模型为百炼 `qwen3.7-flash`（flash 档，**不是**项目默认的 `qwen-max`）；采集日期 2026-09-11；**整轮耗时 14 min 25 s**。
- **原始数据**：`metrics/ai-baseline-2026-09-11T03-30-25-191Z.json`（本地归档，`metrics/` 不入库），同目录 `.summary.md` 由脚本自动生成。
- **对比起点**：功能等价于 tag `ai-baseline-v1` → `514e453`；JS 产物相对 `514e453` 从 843848 → 843861 B（+13 B 来自 `handleSend` 暴露，harness 本身不进 bundle）。

### 8.1 总体（25 轮 / 74 次请求 / 96 次工具调用）

| 指标 | 值 |
|---|---|
| 轮次结果 | 成功 7（28%）/ 部分成功 13（52%）/ 失败 5（20%） |
| 请求成功率 | 93.2%（74 次成功 69，失败全部是 timeout） |
| 工具调用成功率 | 40.6%（96 次，成功 39） |
| 参数解析 | 正常 45（46.9%）/ 修复 0 / **不可用 51（53.1%）** |
| 重试 | 平均 1.36 次/轮，最多 2 次；39/74 次请求（52.7%）带重试 |
| 单轮延迟（成功请求） | P50 3.19 s / P95 11.48 s / 最大 17.15 s |
| Token | 69 次请求带 usage；平均 prompt 5169 / completion 149；累计 366,989（≈14,680/轮） |
| Prompt 注入 | 平均 4,615 字符（≈3551 tokens）；工具 schema 固定 9,387 字符/请求 |
| 每轮请求数 | 平均 2.96（= 一次对话平均要发近 3 个 HTTP 请求） |

### 8.2 分场景

「命中率」= 该轮确实调用了期望工具的比例；「轮次成功率」= 整轮 `outcome = success` 的比例；延迟只统计成功请求。

| 场景 | 命中率 | 轮次成功率 | 平均轮次 | 平均重试 | 工具调用/失败 | P50 | P95 | tokens |
|---|---|---|---|---|---|---|---|---|
| `generate-resume` | 5/5 | 0% | 3 | 2（最多 2） | 19/19 | 3185 ms | 7051 ms | 75684 |
| `add_module` | 5/5 | 40% | 4.8 | 1.2（最多 2） | 44/7 | 3979 ms | 12702 ms | 129318 |
| `set_content` | 2/5 | 80% | 1.8 | 0.4（最多 2） | 5/3 | 2672 ms | 3530 ms | 40191 |
| `set_style` | 3/5 | 20% | 2.2 | 1.2（最多 2） | 13/13 | 3267 ms | 12165 ms | 45400 |
| `tool-error-retry` | 5/5 | 0% | 3 | 2（最多 2） | 15/15 | 2959 ms | 11481 ms | 76396 |

### 8.3 关键发现

1. **参数解析失败占 53.1%，且全部集中在「参数含嵌套对象」的工具上**：`set_style` 28/28、`execute_skill` 23/23 的 `arguments` 无法解析；而参数为扁平字符串的 `add_module` 24/24、`remove_module` 17/17、`set_content` 3/3 全部解析正常（45 + 51 = 96）。`repairTruncatedJson` 一次都没救回来（`argsRepaired = 0`），说明失败**不是截断**，更像模型输出了非严格 JSON（未加引号的键 / 单引号 / 尾逗号）——需要原始 args 原文确认。
2. **主路径被打穿**：`generate-resume` 场景 5/5 都调用了 `execute_skill`，但 23 次调用全部因参数不可解析退化成 `未知技能: undefined`（`unknown_skill`），该场景轮次成功率 0%（用户视角就是「让 AI 预设一份简历」5 次全失败）。
3. **重试没有自愈能力**：39/74 次请求（52.7%）是重试，平均 1.36 次/轮，但**参数修复成功次数为 0**。当前回传给模型的是固定文案「请修正错误并重试。」，没有把「解析失败 + 原始 args 片段」带回去，模型大概率重复同一个错误。
4. **`add_module` 场景出现 add→remove 抖动**：17 次 `remove_module`（其中 5 次 handler_error）；唯一一次跑满 `MAX_TOOL_ROUNDS = 8` 的是该场景第 1 轮（35.7 s、51.5k tokens）。
5. **timeout 是墙钟与成本的主要消耗**：5 次请求卡到 120 s 上限（合计 600 s，占整轮 865 s 的 69%），其中一次还是首轮、prompt 估算仅 ≈2.5k tokens 的请求；成功请求的 P95 只有 11.48 s，说明 120 s 的单请求上限过宽。
6. **成本结构**：平均 14,680 tokens/轮，prompt 占 97.2%（5169 vs 149 completion）；每轮固定注入工具 schema 9,387 字符，是最大的一笔固定开销。
7. **「轮次成功率」会被「只回文本」虚高**：`set_content` 有 3 轮完全没调用工具、仅文本回复，却因无工具报错被记为 `success`（这也是该场景命中率只有 2/5 的原因）。所以判定 AI 链路必须同时看命中率与工具成功率，不能只看轮次成功率。
8. **失败场景被掩盖**：本应触发 `MODULE_NOT_FOUND` 结构化错误的 `tool-error-retry` 场景，实际没验到——`set_style` 在参数解析阶段就失败了。真正走到「结构化错误 → 提示重试」链路的只有 `remove_module` 的 5 次 handler_error，且 5 次均未自愈。

### 8.4 对比口径

- `before = 8d94975`（本节的 25 轮基线）；`after = <修复提交>`。两侧必须**同 harness、同模型、同机器、同 5 类场景**，判定看：工具成功率、参数不可用率、重试次数/请求、P50/P95、每轮 tokens。
- **模型档位必须固定**：03-24 的一次对照用了不存在的 `qwen3.8-flash`，5/5 请求返回 HTTP 400、整轮无数据；03-27 用 `qwen3.7-flash` 单轮对照**复现了同一现象**（20 次调用里 12 次参数不可用，`set_style` 8/8、`execute_skill` 4/4 全失败），说明本次发现可复现，不是单次噪声。

### 8.5 后续修复计划（按性价比排序）

| 优先级 | 改动 | 为什么 | 验收（用同一 harness 复跑） |
|---|---|---|---|
| P0 | 参数解析加容错：`JSON.parse` → `repairTruncatedJson` → **`JSON5.parse`**（`json5` 已是依赖，`skillExecutor.ts` 已在用）→ 仍失败才记 `invalid_args` | 直接对着 51/96 的失败面 | `argsInvalid` 从 53.1% 降到 <5%，工具成功率 40.6% → >80% |
| P0 | 解析失败时把「失败原因 + 原始 args 前 200 字符」作为 tool result 回传，替换固定文案「请修正错误并重试。」 | 39 次重试 0 次自愈，反馈信息不足 | 同场景重试后参数合法率上升，`retries.avgPerTurn` 下降 |
| P1 | 单请求超时可配置并下调（如 120 s → 45 s），且 timeout 不占满重试额度 | 5 次卡死吃掉 69% 墙钟；成功请求 P95 仅 11.5 s | 采集总时长下降；timeout 不再拖满 2 次重试 |
| P1 | 模型对照：用 `qwen-plus` / `qwen-max` 跑同一 harness | 判定「模型档位」与「链路」各占多少责任 | 输出两份摘要，与 flash 档并列记录 |
| P1 | 重复调用治理：`add_module` 增加同名模块守卫，返回结构化 redundant 提示（对齐已有的 `createdIds_` 守卫） | add→remove 抖动白烧 17 次调用，单轮最高 51.5k tokens | `add_module` 场景工具调用次数与 tokens 下降 |
| P2 | 工具 schema 瘦身（`execute_skill` 的 description 内嵌长参数说明，schema 共 9,387 字符/请求） | 固定 prompt 开销，占 tokens 绝对大头 | 每轮 prompt tokens 下降 |
| P2 | schema 收敛：给 `style` / `params` 定义明确 `properties`，或用扁平参数（`[{key, value}]`）替代嵌套对象 | 若发现 1 成立，嵌套深度就是模型出错的诱因 | 参数不可用率进一步下降 |

**P0 前置（先取证）**：取一份模型原始 args 原文——终端里 `[Tool Call] 参数 JSON 不合法且无法修复: …` 后面就是原文前 200 字符（`npm run ai-baseline` 的 verbose reporter 会打印；也可用 `AI_BASELINE_ITERATIONS=1` 单场景复跑抓一行）。

## 九、后续待办

- [x] `vitest bench`：覆盖 `layoutEngine` / `layoutTreeNormalizer` / `moduleUtils` 纯函数热路径（见第六节）
- [x] AI 调用埋点：成功率、重试轮数、单轮耗时、token 消耗、每轮注入体积（见第七节）
- [x] 跑 AI 基线：25 次场景化调用，覆盖 5 类场景（含 1 次失败场景），结果见第八节
- [ ] AI 链路修复：按第八节 8.5 的 P0 → P1 顺序推进，每步用同一 harness 复跑对比
- [ ] harness 卫生：运行 0 轮（未设 Key、或模型 400 全失败）时不落盘或标记为无效，避免 `metrics/` 里出现看着像基线的空文件
- [ ] `scripts/metrics.mjs`：自动采集构建/测试/lint 指标，写 `metrics/<sha>.json` 并由脚本生成第五节的表格
